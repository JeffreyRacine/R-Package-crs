This archive records the `crs` Phase 1 package-resource modernization tranche
run on 2026-03-21.

The legacy package-shipped vignette sources were removed from the live
`vignettes/` surface so the package can ship a lean installed getting-started
vignette while the gallery remains the long-form teaching surface.

Archived legacy vignette sources:

- `crs.Rnw`
- `crs.bib`
- `crs_faq.Rnw`
- `crs_faq.bib`
- `spline_primer.Rnw`
- `spline_primer.bib`

The follow-on startup-banner cleanup tranche should update any remaining
references to retired vignette names after the new installed vignette surface is
in place and validated.
